[project]
name = "db-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "mohammedarbaz119", email = "mohammedarbazq16@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "asyncpg>=0.31.0",
    "langchain-cloudflare>=0.3.9",
    "langchain-community>=0.4.2",
    "langchain-nvidia-ai-endpoints>=1.4.3",
    "langgraph>=1.2.11",
    "python-dotenv>=1.2.3",
]

[project.scripts]
db-agent = "db_agent:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
